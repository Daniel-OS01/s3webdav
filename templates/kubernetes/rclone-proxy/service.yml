apiVersion: v1
kind: Service
metadata:
  name: rclone-proxy
  namespace: rclone-proxy
  labels:
    app: rclone-proxy
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: rclone-proxy
