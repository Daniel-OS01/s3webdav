# ---------------------------------------------------------------------------
#                      --- General Settings ---
#
# These variables are used by multiple services.
# ---------------------------------------------------------------------------

# Specify the user and group IDs to run the containers as.
# This prevents permission issues with mounted volumes.
# Find yours by running `id -u` and `id -g` in your terminal.
PUID=1000
PGID=1000

# Specify the timezone for the containers.
# A list of valid timezones can be found here: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Etc/UTC


# ---------------------------------------------------------------------------
#                      --- Homarr Settings ---
#
# Configuration for the Homarr dashboard service.
# ---------------------------------------------------------------------------

# A 64-character hexadecimal string used for data encryption.
# You can generate one with the command: openssl rand -hex 32
SECRET_ENCRYPTION_KEY=

# The host port to map to Homarr's internal port (7575).
# This is optional and defaults to 7575 if not set.
HOMARR_PORT=7575


# ---------------------------------------------------------------------------
#                      --- Duplicati Settings ---
#
# Configuration for the Duplicati backup service.
# ---------------------------------------------------------------------------

# A unique encryption key for the settings database.
# Generate a random string for this value.
SETTINGS_ENCRYPTION_KEY=

# The password for the Duplicati web interface.
# This is optional but highly recommended for security.
DUPLICATI__WEBSERVICE_PASSWORD=


# ---------------------------------------------------------------------------
#                      --- LiteLLM Settings ---
#
# Configuration for the LiteLLM proxy service.
# These values are referenced by the litellm_config.yaml file.
# ---------------------------------------------------------------------------

# Your API key for the Azure OpenAI service.
AZURE_API_KEY=

# The base URL for your Azure OpenAI API endpoint.
# e.g., https://your-azure-openai.openai.azure.com/
AZURE_API_BASE=
